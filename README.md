# A Java-based Gomoku AI

This is an AI of Gomoku (Gobang) based on DFS. The following techniques are employed in AI calculation:

+ Game tree
+ Alpha-beta pruning 
+ Heuristic algorithm
+ Score cache
+ *VCS* (In development)

**The ```cmdGame.java``` is the main entry of program**

